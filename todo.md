# Roadmap

* placeholders - largest first
* templates.generate
  * each element MUST specify items
  * omitted index: results in generating a single pagination template with those items.
* numerical range indexing